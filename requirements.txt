# Core
numpy>=1.24.0
opencv-python-headless>=4.8.0
Pillow>=10.0.0
tqdm>=4.66.0
python-dotenv>=1.0.0
PyYAML>=6.0

# YOLOv8
ultralytics>=8.1.0

# Video / pose / emotion (aulas FIAP)
# MediaPipe clássico (mp.solutions) — se a instalação trazer 1.x, o código usa fallback OpenCV
mediapipe>=0.10.9,<0.11; python_version < "3.13"
# deepface is heavy; install optionally: pip install deepface tf-keras
# face_recognition optional (needs dlib)

# Audio (aula 04)
moviepy>=1.0.3
SpeechRecognition>=3.10.0
pydub>=0.25.1

# Text / ML (aulas 05-06)
scikit-learn>=1.3.0
nltk>=3.8.0

# Cloud (AWS + OpenAI) — optional at runtime with local fallbacks
boto3>=1.34.0
openai>=1.12.0

# Reports
reportlab>=4.0.0
